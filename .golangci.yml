run:
  concurrency: 4
  timeout: 5m

linters-settings:
  goimports:
    local-prefixes: "github.com/GBA-BI/tes-api"
  goconst:
    min-len: 2
    min-occurrences: 5

linters:
  disable-all: true
  enable:
  - ginkgolinter
  - gocritic
  - govet
  - goconst
  - gofmt
  - goimports
  - ineffassign
  - misspell
  - unparam
  - unused

issues:
  exclude-use-default: false
  exclude-rules:
  - path: _test.go
    linters:
    - errcheck
